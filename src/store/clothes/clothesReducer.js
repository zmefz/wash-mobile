const initialState = {
  list: [],
  loading: false,
  error: null,
}

export default function clothesReducer(state = initialState, action) {
  switch (action) {
    default:
      return state
  }
}
